#!/bin/bash

#Checking what happend when i run this command
docker run -i -t ubuntu /bin/bash

#This command:
#1. Create a docker container
#2. Download ubuntu image
#3. Run in docker container bash shell
#4. After that i use a command exit but my container still is created